pipeline {
    agent {
        node {
            label 'maven'
        }
    }
environement{ PATH = "/opt/apache-maven-3.8.8/bin: $PATH "
}
    stages {
        stage('Build') {
            steps {
               sh 'mvn clean deploy'
            }
        }
    }
}
